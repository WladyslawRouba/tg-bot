{
  "name": "fastify-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "set-webhook": "node --env-file=.env set-webhook.js",
    "tunnel": "cloudflared tunnel --url http://localhost:3000",
    "test": "node --test"
  },
  "dependencies": {
    "fastify": "^5.12.3"
  }
}
